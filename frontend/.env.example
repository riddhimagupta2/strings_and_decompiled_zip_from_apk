# Local dev: leave empty — Vite proxies /api to the backend (see .env.development)
VITE_API_BASE_URL=

# Vite dev proxy target (only used when npm run dev)
VITE_PROXY_TARGET=http://127.0.0.1:8000

# Production build example:
# VITE_API_BASE_URL=https://your-api.example.com
